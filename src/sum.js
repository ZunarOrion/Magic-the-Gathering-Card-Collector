// export function sum(a, b) {
//     return a + b
// };


// export function validate(formData) {
//     const blogTitle = formData.blogTitle;
//     const blogText = formData.blogText;
//     if (!blogTitle === typeof "string" || !blogText === typeof)
// };